{
  "name": "nusaquant-futures-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Research and paper-trading platform for Binance USDⓈ-M Futures",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @nusaquant/web",
    "build": "npm run build --workspace @nusaquant/web",
    "start": "npm run start --workspace @nusaquant/web",
    "typecheck": "npm run typecheck --workspace @nusaquant/web && npm run typecheck --workspace @nusaquant/worker",
    "test": "npm run test --workspace @nusaquant/core && npm run test --workspace @nusaquant/worker"
  }
}
