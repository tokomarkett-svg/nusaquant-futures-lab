{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo skip-build: worker jalan langsung via tsx, tanpa kompilasi"
  },
  "deploy": {
    "startCommand": "npm run start --workspace @nusaquant/worker",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
